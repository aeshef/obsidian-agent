---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
platform: "{{ platform }}"
handle: "{{ handle }}"
topics:
  {% for t in (topics or []) %}- {{ t }}
  {% endfor %}
quality: {{ quality }}
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Платформа: {{ platform }}
- Аккаунт/ссылка: {{ handle }}
- Исходные файлы: {{ raw_dir }}


