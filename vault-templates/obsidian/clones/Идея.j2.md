---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
{% set _files = (attachments.files or []) %}
{% set _links = (attachments.links or []) %}
{% if _files %}
files:
  {% for f in _files %}- "{{ f }}"
  {% endfor %}
{% endif %}
{% if _links %}
links:
  {% for l in _links %}- "{{ l }}"
  {% endfor %}
{% endif %}
source: "{{ source }}"
form: "{{ form }}"
scope: "{{ scope }}"
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Гипотеза
- 

## Следующие шаги
- 


