---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
source: "{{ source }}"
form: "{{ form }}"
scope: "{{ scope }}"
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Гипотеза
- 

## Следующие шаги
- 


