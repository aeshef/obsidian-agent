---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
form: "{{ form }}"
doc_type: "{{ doc_type }}"
expires: "{{ expires }}"
owner: "{{ owner }}"
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}


