---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
form: "{{ form }}"
latitude: {{ latitude }}
longitude: {{ longitude }}
address: "{{ address }}"
city: "{{ city }}"
category: "{{ category }}"
event_date: "{{ event_date }}"
event_time: "{{ event_time }}"
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Заметки о месте
- 


