---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
source: "{{ source }}"
form: "{{ form }}"
cuisine: "{{ cuisine }}"
kind: "{{ kind }}"
time: "{{ time }}"
ingredients:
  {% for ing in (ingredients or []) %}- {{ ing }}
  {% endfor %}
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Ингредиенты
- 

## Шаги
1. 


