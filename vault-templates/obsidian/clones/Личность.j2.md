---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
traits:
  {% for tr in (traits or []) %}- {{ tr }}
  {% endfor %}
inspirations:
  {% for insp in (inspirations or []) %}- {{ insp }}
  {% endfor %}
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Мудборд
- 


