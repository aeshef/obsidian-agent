---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
source: "{{ source }}"
form: "{{ form }}"
status: "{{ status }}"
author: "{{ author }}"
rating: {{ rating }}
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

- Исходные файлы: {{ raw_dir }}

## Конспект
- 


