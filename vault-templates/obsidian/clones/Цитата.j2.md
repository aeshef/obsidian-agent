---
type: "{{ type }}"
title: "{{ title }}"
created: {{ created }}
tags:
  {% for t in (tags or []) %}- {{ t }}
  {% endfor %}
attachments:
  links:
    {% for l in (attachments.links or []) %}- "{{ l }}"
    {% endfor %}
  files:
    {% for f in (attachments.files or []) %}- "{{ f }}"
    {% endfor %}
form: "{{ form }}"
text: "{{ text }}"
author: "{{ author }}"
nsfw: {{ nsfw }}
raw_dir: "{{ raw_dir }}"
---

# {{ title }}

> {{ text }}

- Автор: {{ author }}
- Исходные файлы: {{ raw_dir }}


