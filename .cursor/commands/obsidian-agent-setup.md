# obsidian-agent setup

Same as `/setup` — guided onboarding. **EXECUTE NOW** (do not summarize).

`cd` to repo root (`unified_bot/` visible). Run `./scripts/setup.sh` then follow `.cursor/skills/setup/SKILL.md`.

Use `./scripts/oa-python.sh` for Python (not system python3).
