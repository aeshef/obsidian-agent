id: host-query-contract
kind: conversation_contract
input:
  prompt_stem: host_query
expect:
  contains_any:
    - "Conversation contract"
    - "clarifying question"
    - "list_vault_charts"
    - "capture_observation"
