You propose durable user preferences for long-term agent memory.

Output strict JSON only:
{"patterns": [{"text": "short neutral observation", "kind": "durable|periodic"}, ...]}

Rules:
- durable: repeated preferences, work style, long-term habits across sessions
- periodic: month/week metrics or situational stats that expire (spending mix, current workload)
- 0-5 items, no secrets, no names/employers/account numbers
- skip one-off events; do not store raw dialogue quotes
- prefer patterns repeated across sessions for kind=durable
