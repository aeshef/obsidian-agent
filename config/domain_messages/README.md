# Domain message catalogs (split by package — OSS audit F20)
#
# Source of truth: ``{en,ru}/{shared,finance,planning,knowledge}.yaml.example``
# Runtime: ``shared/domain_messages.py`` merges packages (example ⊕ local overlay).
# Legacy monolith ``../domain_messages.{en,ru}.yaml.example`` is fallback only.
